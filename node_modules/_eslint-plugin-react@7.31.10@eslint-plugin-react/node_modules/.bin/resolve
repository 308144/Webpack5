#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_resolve@2.0.0-next.4@resolve/bin/resolve" "$@"
  ret=$?
else 
  node  "$basedir/../../../_resolve@2.0.0-next.4@resolve/bin/resolve" "$@"
  ret=$?
fi
exit $ret
